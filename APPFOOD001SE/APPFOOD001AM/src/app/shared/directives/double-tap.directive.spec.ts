import { DoubleTapDirective } from './double-tap.directive';

describe('DoubleTapDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubleTapDirective();
    expect(directive).toBeTruthy();
  });
});
